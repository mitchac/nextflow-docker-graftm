profiles {
  standard {
        process {
      withName:graftm_graft {
        container = 'test:latest'
      }
    }
  }
  
  docker {
    docker.enabled = true
    tmp = '/home/debian/nextflow-docker-graftm'
  }
}
