profiles {
  standard {
        process {
      withName:graftm_graft {
        container = 'graftm:latest'
      }
    }
  }
  
  docker {
    docker.enabled = true
    tmp = '/home/debian/nextflow-docker-graftm'
  }
}
